strict digraph  {
misc;
mask;
ls;
compare;
mask_ls;
io;
binary;
memory;
calc;
convert;
mask -> misc;
mask -> mask_ls;
mask -> calc;
ls -> misc;
ls -> binary;
ls -> mask_ls;
ls -> mask;
ls -> calc;
mask_ls -> compare;
convert -> calc;
}
